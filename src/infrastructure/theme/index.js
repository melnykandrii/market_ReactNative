import { colors } from "./colors";
import { space, lineHeights } from "./spacing";
import { sizes, sizepx } from "./sizes";
import { fonts, fontWeights, fontSizes, fontSize } from "./fonts";

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  sizepx,
  fonts,
  fontSizes,
  fontSize,
  fontWeights,
};
